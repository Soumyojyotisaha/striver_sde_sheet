#include<bits/stdc++.h>
using namespace std;

struct TreeNode
{
    int data;
    TreeNode* left;
    TreeNode* right;
    TreeNode(int val):data(val),left(nullptr),right(nullptr){}
};
class Solution
{
    public:
void check(vector<int> &nums)
{
return 0;
}

    private:
};

int main()
{
    TreeNode* node=new TreeNode(1);
    Solution sol;
vector<int>arr={1,2,3};
    cout<<sol.check(arr)<<end;
    return 0;
}